<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@color/TextColor</item>
        <item name="android:textColor">@color/TextColor</item>
    </style>
    <style name="dashboard_row_style">
        <item name="android:layout_height">100dip</item>
    </style>
    <style name="dashboard_button_style">
        <item name="android:layout_width">0dip</item>
        <item name="android:layout_height">100dip</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:layout_margin">7dip</item>
        <item name="android:textColor">@color/ButtonTextColor</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="TextEditor">
        <item name="android:textColor">#CCCCCC</item>
    </style>
    <style name="DatePickerStyle" parent="TextEditor">
        <item name="android:editTextStyle">@color/TextColor</item>
    </style>
    <style name="ButtonStyle">
        <item name="android:textColor">#DDDDDD</item>
    </style>
    <color name="TextColor">#CCCCCC</color>
    <color name="ButtonTextColor">#DDDDDD</color>

</resources>
