<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DashboardActivity" >

    <TextView
        android:id="@+id/helloWorldView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />


            <ListView
                android:layout_below="@+id/helloWorldView"
                android:id="@+id/quickHistory"
                android:layout_width="match_parent"
                android:layout_height="100dp" >
            </ListView>


    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignLeft="@+id/quickHistory"
        android:layout_below="@+id/quickHistory" android:id="@+id/dashboardButtonFrame">

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:columnCount="2" >

            <Button
                android:id="@+id/loanButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableTop="@drawable/ic_launcher"
                android:onClick="onLoanButtonClick"
                android:text="Loan" />
            <Button
                android:id="@+id/borrowButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableTop="@drawable/ic_launcher"
                android:onClick="onBorrowButtonClick"
                android:text="Borrow" />
                        <Button
                android:id="@+id/historyButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableTop="@drawable/ic_launcher"
                android:onClick="onHistoryButtonClick"
                android:text="History" />
                        <Button
                android:id="@+id/libraryButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableTop="@drawable/ic_launcher"
                android:onClick="onLibraryButtonClick"
                android:text="Library" />
            </GridLayout>

    </FrameLayout>



</RelativeLayout>
